# homebrew-sipctl

Homebrew tap for [sipctl](https://github.com/vyshnavsdeepak/sipctl) — the `sip` MCP server.

## Install

```bash
brew tap vyshnavsdeepak/sipctl
brew install sipctl
```

## Use with Claude Code

```json
{ "mcpServers": { "sip": { "command": "sip" } } }
```
